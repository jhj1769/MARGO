You are about to issue a directive for the current recommendation session.

## Operator brief (free-form)
{{ brief }}

## Domain vocabulary (use ONLY these when boosting/forbidding categories or attributes)
{% for bucket, terms in vocabulary.items() -%}
- {{ bucket }} (showing up to 40 of {{ terms | length }}): {{ terms[:40] | tojson }}
{% endfor %}

Return JSON matching the MARGO Directive schema:
{
  "goal": "...",
  "structured_constraints": { ... },
  "natural_language": "..."
}
